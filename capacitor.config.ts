import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.travelGuide.com',
  webDir: 'dist/my-metro-app', 
  appName: 'Mumbai Metro App',
};

export default config;
